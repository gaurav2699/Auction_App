<h1>Editing Auctionpost</h1>

<%= form_with(model: @auctionpost, local: true) do |form| %>
  <% if @auctionpost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@auctionpost.errors.count, "error") %> prohibited this auctionpost from being saved:</h2>

      <ul>
      <% @auctionpost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>
  <div class="field">
    <%= form.label :currentbid %>:
    <%= form.label @auctionpost.startingbid%>
  </div>

  <div class="field">
    <%= form.label :deadline %>
    <%= form.datetime_select :deadlinetime %>
  </div>
  <div class="field">
    <%= form.label :contactnumber %>
    <%= form.number_field :contactnumber %>
  </div>
  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= link_to 'Show', @auctionpost %> |
<%= link_to 'Back', auctionposts_path %>
